import CarpoolPage from "@/components/NotScopedforv1/CarComponents/CarpoolPage";

/**
 * Route for matching the path /carpool and render the UI for the carpool pagee
 */
export default function Carpool() {
  return <CarpoolPage />;
}
