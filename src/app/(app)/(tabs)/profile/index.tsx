import ProfileView from "@/features/Profile/Views/ProfileView";

/** Render the Profile Page which internally gets current user profile */
export default function Profile() {
  return <ProfileView />;
}
