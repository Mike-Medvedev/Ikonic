import ProfileView from "@/features/Profile/Views/ProfileView";

/**
 * Route for profile which displays current users profile at /profile
 */
export default function Profile() {
  return <ProfileView />;
}
